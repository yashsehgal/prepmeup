name: Adds all issues to the PrepMeUp project board

on:
  issues:
    types:
      - opened

jobs:
  add-to-project:
    name: Add issue to project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project
        with:
          project-url: https://github.com/users/yashsehgal/projects/6
          github-token: ${{ secrets.ADD_TO_PROJECT_PAT }}
